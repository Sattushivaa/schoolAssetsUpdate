customElements.define("img-lightbox", new class extends HTMLImageElement {
    constructor(e){
        super();
        let url = this.url;
        // show large on click
        // background change to dark transparent
        // show cut button
    }
}, {extends:"img"});